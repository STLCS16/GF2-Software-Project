逻辑仿真器 (Logic Simulator)

一个逻辑电路仿真工具。用户用自定义语法写一个"电路定义文件"，工具就能解析并计算出电路中每一个逻辑门（Gate）的输出电平（0 或 1）。

输入：一个 .def 定义文件，描述电路的输入端口和门电路连接关系。
输出：每个门的信号电平。

核心流程

定义文件 → Scanner → Parser → Network → Monitor → 输出信号电平
五个核心模块各司其职：

模块	职责
Scanner	读取文本，拆分成 Token
Parser	根据语法规则构建语法树
Network	建立门之间的连接关系（谁连谁）
Name	管理所有信号和门的名称（查重、解析）
Monitor	按顺序计算每个门的输出电平
我的贡献
独立负责 GUI 模块：为用户提供图形界面，方便加载定义文件、运行仿真并可视化结果

协助队员完成其他模块：参与 Parser 和 scanner 模块的代码审查与调试，配合完成模块间的接口对接与集成测试

技术栈
语言：Python

协作工具：Git

